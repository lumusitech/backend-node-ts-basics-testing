describe('App', () => {
  it('should be 30', () => {
    // Arrange
    const num1 = 20
    const num2 = 10

    // Act
    const result = num1 + num2

    // Assert
    expect(result).toBe(30)
  })
})
